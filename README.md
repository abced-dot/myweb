<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
</head>
<body style="background-color:black;">
    <div style="margin-bottom:0px ">
        <canvas id="c"></canvas>
    </div>
    <script style="margin-top: 0px">
        var b = document.body;
        var c = document.getElementsByTagName('canvas')[0];
        var a = c.getContext('2d');
        document.body.clientWidth;
        with (m = Math)
        C = cos, S = sin, P = pow, R = random;
        // 保持花朵大尺寸基础，聚焦调整叶子参数
        c.width = c.height = f = 700;
        h = -300;
        
        function p(a, b, c) {
            if (c > 60)
                // 关键修改1：还原叶子基础形态 - 频率9→7，幂次5→4，减少过度褶皱；尺寸60→40缩小叶子
                return [S(a * 7) * (12 + 4 / (.2 + P(b * 4, 4))) - S(b * 1.1) * 40,  
                b * f * 0.95 + 40,  // 关键修改2：缩放1.1→0.95，尺寸60→40，缩小叶子并回归对称
                625 + C(a * 7) * (12 + 4 / (.2 + P(b * 4, 4))) + b * 300 * 0.95,  // 关键修改3：450→300，缩小叶子延伸长度
                a * 1.05 - b / 2.2, a * 1.1];  // 关键修改4：调整系数，让叶子轮廓更自然
            
            A = a * 2 - 1;
            B = b * 2 - 1;
            if (A * A + B * B < 1) { 
                if (c > 37) { 
                    n = (j = c & 1) ? 6 : 4;  // 关键修改5：还原角度因子7/5→6/4，回归叶子自然分叉角度
                    // 关键修改6：减少随机扰动R()*2→R()*0.5，调小系数380→280，缩小叶子尺寸；频率150→130，减少过度褶皱
                    o = .55 / (a + 0.02) + C(b * 130) * 3.5 - a * 280 + R()*0.5;  
                    w = b * h * 0.98; 
                    return [o * C(n) + w * S(n) + j * 650 - 390 + R()*0.3,  // 关键修改7：降低随机扰动R()→R()*0.3
                    o * S(n) - w * C(n) + 600 - j * 300 * 1.05,  // 关键修改8：350→300，缩小叶子宽度
                    1180 + C(B * 1.05 + A * 1.05) * 100 - j * 300,  // 关键修改9：非对称1.1/0.9→对称1.05，120→100缩小尺寸
                    .4 - a * 0.11 + P(1 - B * B, -h * 6) * 0.16 - a * b * 0.45 + C(a * 1.05 + b * 1.05) / 4.8 + 
                    P(C((o * (a + 1.05) + (B > 0 ? w : -w)) / 26), 30) * 0.11 * (1 - B * B), 
                    o / 1e3 + 0.72 - o * w * 3.0e-6]; 
                } 
                if (c > 32) { 
                    c = c * 1.12 - 0.15;  // 关键修改10：曲率系数1.18→1.12，减少过度弯曲
                    o = a * 45 - 22 + R()*0.8;  // 关键修改11：58→45缩小尺寸，随机扰动1.5→0.8减少不规则
                    w = b * b * h * 1.02; 
                    z = o * S(c * 1.02) + w * C(c * 1.02) + 650;  // 关键修改12：非对称1.05/0.95→对称1.02，680→650缩小尺寸
                    return [o * C(c) - w * S(c), 
                    28 + C(B * 0.7) * 95 - b * b * b * 65 - z / 2.05 - h,  // 关键修改13：110→95、75→65，缩小叶子厚度
                    z, (b * b * 0.32 + P((1 - (A * A)), 7) * 0.15 + 0.3) * b, b * 0.72];  // 关键修改14：调整系数，让叶子更小巧
                } 
                // 关键修改15：还原基础轮廓 - 系数2.2→2.1，95→85，2.1→2.0；幂次9→7，减少过度褶皱
                o = A * (2.1 - b) * (85 - c * 2.0);  
                w = 105 - C(A * 1.05) * 130 - C(b * 1.1) * (-h - c * 4.8) + C(P(1 - b, 7)) * 60 + c * 2.1; 
                z = o * S(c * 1.03) + w * C(c * 1.03) + 750;  // 关键修改16：非对称1.08/0.92→对称1.03，780→750缩小尺寸
                return [o * C(c) - w * S(c), 
                B * 105 - C(P(b, 7)) * 65 - c / 2.9 - z / 1.38 + 500,  // 关键修改17：115→105、75→65，缩小叶子尺寸
                z, (1 - b / 1.22) * 0.9 + a * 0.1, P((1 - b), 20) / 4.0 + 0.05];  // 关键修改18：调整系数，回归自然叶子比例
            }
        } 
        
        // 保持绘制逻辑，确保叶子形态清晰
        setInterval('for(i=0;i<1e4;i++)if(s=p(R(),R(),i%46/.74)){z=s[2];x=~~(s[0]*f/z-h);y=~~(s[1]*f/z-h);if(!m[q=y*f+x]|m[q]>z)m[q]=z,a.fillStyle="rgb("+~(s[3]*h/4)+","+~(s[4]*h/3)+","+~(s[3]*s[3]*-200)+")",a.fillRect(x,y,1,1)}', 0)
    </script>
    <h3>
        <p style="float:right;color:green"></p>
    </h3>
</body>
</html>
